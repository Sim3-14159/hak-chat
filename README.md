# hak-chat
An instant chatting system for multi user POSIX environments (only for the coolest of coders XD).

---

## Installation

All you have to do to install hak-chat to your system is run `make all`. Note that hak-chat only works on POSIX systems.

```bash
make all
```
